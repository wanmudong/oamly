<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.wanmudong.oamly.modules.announce.mapper.AnnounceMapper">

    <select id="selectAnnounceList" resultType="Announce">
        select * from announce where status=1 order by id desc
    </select>

</mapper>
